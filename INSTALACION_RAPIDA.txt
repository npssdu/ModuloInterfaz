╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║   GUÍA RÁPIDA DE INSTALACIÓN                                                ║
║   Proyecto BD - Gabinete de Abogados                                         ║
║   Período 2025-3                                                             ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

================================================================================
PRERREQUISITOS
================================================================================

Antes de iniciar, verifica que tengas instalado:

□ Oracle Database 11g o superior
  → Descarga desde: oracle.com/database
  
□ Python 3.8 o superior
  → Descarga desde: python.org
  → En Windows: ✓ "Add Python to PATH"
  
□ Visual Studio Code o editor preferido
  → Descarga desde: code.visualstudio.com
  
Verificar instalación:
$ python --version
$ pip --version
$ sqlplus -v

================================================================================
PASO 1: CONFIGURAR ORACLE (10-15 minutos)
================================================================================

1.1 - Crear usuario en Oracle
════════════════════════════════

Conectarse a SQL*Plus con usuario SYSTEM:
$ sqlplus system/password@orcl

O usar SQL Developer si prefieres interfaz gráfica.

Ejecutar en SQL*Plus:

SQL> CREATE USER ABOGADO IDENTIFIED BY Abc123456;
SQL> GRANT CONNECT, RESOURCE, DBA TO ABOGADO;
SQL> COMMIT;
SQL> EXIT;

1.2 - Crear tablas y datos
═══════════════════════════

Conectarse con usuario ABOGADO:
$ sqlplus abogado/Abc123456@orcl

En SQL*Plus, ejecutar:
SQL> @01_crear_tablas.sql;
SQL> @02_inserciones_datos.sql;
SQL> COMMIT;

Verificar que funcionó:
SQL> SELECT COUNT(*) FROM CLIENTE;
     -- Debe retornar: 5

SQL> SELECT COUNT(*) FROM ABOGADO;
     -- Debe retornar: 5

SQL> EXIT;

================================================================================
PASO 2: PREPARAR CARPETAS (5 minutos)
================================================================================

Crear estructura de proyecto:

C:\Users\TuNombre\ProyectoGabinete\          ← Carpeta principal
├── proyecto/
│   ├── __init__.py
│   ├── settings.py                          ← Reemplazar con 03_django_settings.py
│   ├── urls.py                              ← Reemplazar con 05_django_urls.py
│   ├── asgi.py
│   └── wsgi.py
│
├── clientes/
│   ├── migrations/
│   │   └── __init__.py
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── models.py
│   ├── tests.py
│   └── views.py                             ← Reemplazar con 04_django_views.py
│
├── templates/
│   └── clientes/
│       └── registro_cliente.html            ← Copiar 06_html_template.html aquí
│
├── static/
├── manage.py
└── requirements.txt

================================================================================
PASO 3: INSTALAR PYTHON PACKAGES (5-10 minutos)
================================================================================

3.1 - Crear entorno virtual
═════════════════════════════

En terminal (cmd en Windows):

$ cd C:\Users\TuNombre\ProyectoGabinete

Crear entorno virtual:
$ python -m venv venv

Activar entorno:

WINDOWS:
$ venv\Scripts\activate

LINUX/MAC:
$ source venv/bin/activate

Resultado esperado: (venv) al inicio de la línea en terminal

3.2 - Instalar dependencias
═════════════════════════════

$ pip install --upgrade pip

$ pip install -r requirements.txt

O instalar manualmente:
$ pip install django==4.2
$ pip install cx-Oracle==8.3.0

Verificar instalación:
$ pip list

Debe mostrar:
  Django           4.2.0
  cx-Oracle        8.3.0

================================================================================
PASO 4: CREAR PROYECTO DJANGO (10 minutos)
================================================================================

4.1 - Crear estructura
═══════════════════════

$ django-admin startproject proyecto .

$ python manage.py startapp clientes

Resultado: Se crean carpetas proyecto/ y clientes/

4.2 - Copiar archivos
═══════════════════════

ARCHIVO ORIGEN                  →  ARCHIVO DESTINO
──────────────────────────────────────────────────────
03_django_settings.py          →  proyecto/settings.py
04_django_views.py             →  clientes/views.py
05_django_urls.py              →  proyecto/urls.py
06_html_template.html          →  templates/clientes/registro_cliente.html

4.3 - Editar settings.py
════════════════════════

En el archivo proyecto/settings.py cambiar:

LINEA: INSTALLED_APPS
Cambiar de:
  INSTALLED_APPS = [
      'django.contrib.admin',
      ...
  ]

A:
  INSTALLED_APPS = [
      'django.contrib.admin',
      'django.contrib.auth',
      'django.contrib.contenttypes',
      'django.contrib.sessions',
      'django.contrib.messages',
      'django.contrib.staticfiles',
      'clientes',  ← AGREGAR ESTA LÍNEA
  ]

LINEA: DATABASES
Cambiar "default" a:
  DATABASES = {
      'default': {
          'ENGINE': 'django.db.backends.oracle',
          'NAME': 'localhost:1521/orcl',
          'USER': 'abogado',              ← Cambia según tu usuario
          'PASSWORD': 'Abc123456',        ← Cambia según tu contraseña
      }
  }

LINEA: TEMPLATES DIRS
Asegúrate que exista:
  'DIRS': [os.path.join(BASE_DIR, 'templates')],

Guardar archivo.

================================================================================
PASO 5: CREAR CARPETAS NECESARIAS (2 minutos)
================================================================================

En el proyecto, crear carpeta para templates:

Windows (cmd):
$ mkdir templates
$ mkdir templates\clientes

Linux/Mac:
$ mkdir -p templates/clientes

Luego copiar 06_html_template.html a templates/clientes/registro_cliente.html

================================================================================
PASO 6: PROBAR CONEXIÓN (5 minutos)
================================================================================

6.1 - Verificar conexión Oracle
═════════════════════════════════

$ python manage.py shell

En el intérprete Python:
>>> import cx_Oracle
>>> dsn = cx_Oracle.makedsn('localhost', 1521, service_name='orcl')
>>> connection = cx_Oracle.connect('abogado', 'Abc123456', dsn)
>>> cursor = connection.cursor()
>>> cursor.execute("SELECT COUNT(*) FROM CLIENTE")
>>> print(cursor.fetchone())
(5,)  ← Si imprime 5, la conexión funciona
>>> exit()

6.2 - Crear tablas de Django (opcional)
════════════════════════════════════════

$ python manage.py migrate

(Si sale error, no importa - no estamos usando Django ORM)

================================================================================
PASO 7: INICIAR SERVIDOR (2 minutos)
================================================================================

Asegúrate de estar en la carpeta del proyecto y con el entorno activo:

$ python manage.py runserver

Salida esperada:
  Watching for file changes with StatReloader
  Performing system checks...
  System check identified no issues (0 silenced).
  Quit the server with CONTROL-C.

  * Serving Flask app 'manage'
  * Running on http://127.0.0.1:8000/

✅ El servidor está corriendo.

================================================================================
PASO 8: USAR LA APLICACIÓN (Interactivo)
================================================================================

8.1 - Acceder a la aplicación
══════════════════════════════

En navegador, ir a:
http://127.0.0.1:8000/

Debe cargar la interfaz web de "Registro Cliente"

8.2 - Buscar cliente existente
═══════════════════════════════

1. En el campo "Código Cliente" ingresa: C0001
2. Presiona el botón 🔎 Buscar
3. Se cargarán automáticamente los datos del cliente

Datos esperados para C0001:
  Código: C0001
  Nombre: Juan
  Apellido: Pérez García
  Documento: 1234567890
  Tipo Doc: CC (Cédula de Ciudadanía)

8.3 - Guardar nuevo cliente
═════════════════════════════

1. Limpia el formulario (botón 🔄 Limpiar)
2. Completa los campos:
   Código: C0010
   Nombre: Pedro
   Apellido: González López
   Documento: 5678901234
   Tipo Doc: CC

3. Presiona 💾 Guardar

Debe aparecer mensaje: "Cliente C0010 registrado correctamente"

8.4 - Verificar en BD
════════════════════

En SQL*Plus o SQL Developer:

SQL> SELECT * FROM CLIENTE WHERE codCliente = 'C0010';

Debe mostrar el cliente que acabas de crear.

================================================================================
PASO 9: DETENER SERVIDOR (1 minuto)
================================================================================

En la terminal donde corre el servidor:
Presionar: CTRL + C

Responder: y (si pregunta)

Salida esperada:
  Keyboard interrupt received, exiting.

================================================================================
✅ VERIFICACIÓN FINAL
================================================================================

Para confirmar que todo funciona:

□ 1. Servidor Django corriendo en http://127.0.0.1:8000/
□ 2. Interfaz web visible y responsiva
□ 3. Búsqueda funciona con cliente C0001
□ 4. Puede guardar nuevo cliente
□ 5. Datos aparecen en BD Oracle
□ 6. Sin errores en terminal

Si todos los checks pasan: ✅ ¡LISTO PARA SUSTENTACIÓN!

================================================================================
🆘 PROBLEMAS COMUNES Y SOLUCIONES
================================================================================

PROBLEMA: "Connection refused"
SOLUCIÓN: 
  1. Verificar Oracle corriendo: lsnrctl status
  2. Iniciar Oracle: net start OracleServiceORCL (Windows)
  3. O: sqlplus -v (debe retornar versión)

PROBLEMA: "ORA-28001: the password has expired"
SOLUCIÓN:
  En SQL*Plus:
  SQL> ALTER USER abogado IDENTIFIED BY NuevaPassword;
  SQL> COMMIT;
  Luego actualizar contraseña en settings.py

PROBLEMA: "Template not found"
SOLUCIÓN:
  1. Verificar carpeta: templates/clientes/
  2. Nombre archivo: registro_cliente.html
  3. No diferencia mayúsculas en Linux

PROBLEMA: "Module 'django' has no attribute 'setup'"
SOLUCIÓN:
  En Python shell dentro de Django:
  Usar: python manage.py shell

PROBLEMA: "Port 8000 already in use"
SOLUCIÓN:
  $ python manage.py runserver 8001
  (Usar otro puerto como 8001, 8002, etc)

PROBLEMA: "No module named 'cx_Oracle'"
SOLUCIÓN:
  $ pip install cx-Oracle
  Verificar: pip list | grep cx-Oracle

================================================================================
📞 SOPORTE Y REFERENCIAS
================================================================================

Documentación oficial:
- Django: https://docs.djangoproject.com/
- cx_Oracle: https://cx-oracle.readthedocs.io/
- Oracle: https://docs.oracle.com/

Archivos de referencia incluidos:
- 07_guia_implementacion.txt: Guía detallada
- 08_consultas_sql.sql: Ejemplos SQL
- README.md: Manual del usuario

Profesor: Sonia Ordoñez Salinas
Universidad: Universidad Distrital Francisco José de Caldas

================================================================================
🎉 ¡LISTO PARA EMPEZAR!
================================================================================

Tiempo total estimado: 45 minutos

Si seguiste todos los pasos, tu aplicación debe estar:
✅ Conectada a Oracle
✅ Mostrando interfaz web
✅ Buscando clientes desde BD
✅ Guardando nuevos clientes
✅ Lista para sustentación

¿Tienes dudas? Consulta la guía de implementación (07_guia_implementacion.txt)

¡Éxito! 🚀
================================================================================
